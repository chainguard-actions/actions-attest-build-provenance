name: Test predicate-path input
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Create predicate file
        run: |
          mkdir -p /tmp/attest-test
          cat > /tmp/attest-test/predicate.json <<'EOF'
          {
            "buildType": "https://example.com/build/v1",
            "builder": {
              "id": "https://example.com/builder"
            },
            "invocation": {
              "configSource": {}
            }
          }
          EOF

      - id: action
        uses: .
        continue-on-error: true
        with:
          subject-digest: "sha256:a948904f2f0f479b8f936f68f7b7b5b5b5b5b5b5b5b5b5b5b5b5b5b5b5b5b5"
          subject-name: "test-artifact"
          predicate-type: "https://example.com/build/v1"
          predicate-path: "/tmp/attest-test/predicate.json"
          show-summary: "false"

      - name: Assert action ran (outputs set, or failed gracefully on the sentinel repo)
        env:
          BUNDLE_PATH: ${{ steps.action.outputs.bundle-path }}
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          echo "bundle-path: $BUNDLE_PATH"
          if [ "$OUTCOME" = "success" ]; then
            if [ -z "$BUNDLE_PATH" ]; then
              echo "FAIL: action succeeded but bundle-path output is empty"
              exit 1
            fi
            echo "PASS: attestation created, bundle-path=$BUNDLE_PATH"
          elif [ "$OUTCOME" = "failure" ]; then
            echo "PASS: action failed gracefully (sentinel repo is not resolvable)"
          else
            echo "FAIL: unexpected outcome '$OUTCOME'"
            exit 1
          fi
